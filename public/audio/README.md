Place your background music file here as `bg-music.mp3`.

Supported formats: MP3 is recommended for broad browser support. You can also add `bg-music.ogg` for extra compatibility and include a <source> fallback in the <audio> element if desired.

Make sure you have the rights to use the track.
